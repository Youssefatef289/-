## 2026-07-05 - Contact Form Mobile UX
**Learning:** In Arabic-language real estate landing pages, using `type="tel"` instead of `type="number"` for phone inputs is critical for triggering the correct mobile keyboard that supports '+' and RTL-friendly numeric entry.
**Action:** Always prefer `type="tel"` and include `autocomplete="tel"` for internationalized phone fields.
