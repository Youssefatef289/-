## 2024-05-24 - Explicit Utility Definitions for Accessibility
**Learning:** Relying on framework-provided utility classes like `.sr-only` in legacy projects can lead to visual regressions if those classes are missing or defined only in minified/external assets.
**Action:** Always explicitly define essential accessibility utilities like `.sr-only` in the project's main stylesheet to ensure consistent behavior.
